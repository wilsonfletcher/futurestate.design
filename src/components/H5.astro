---
interface Props {
  tag?: string
  className?: string
}

const { tag = 'h5', className, ...props } = Astro.props as Props
const Tag = tag
---

<Tag
  class:list={['font-bold text-xl leading-tight lg:text-[28px]', className]}
  {...props}
>
  <slot></slot>
</Tag>
