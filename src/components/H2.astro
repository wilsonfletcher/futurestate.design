---
interface Props {
  tag?: string
  className?: string
}

const { tag = 'h2', className, ...props } = Astro.props as Props
const Tag = tag
---

<Tag
  class:list={[
    'font-extrabold text-[40px] leading-tight lg:text-6xl',
    className
  ]}
  {...props}
>
  <slot></slot>
</Tag>
