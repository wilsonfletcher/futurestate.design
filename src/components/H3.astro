---
interface Props {
  tag?: string
  className?: string
}

const { tag = 'h3', className, ...props } = Astro.props as Props
const Tag = tag
---

<Tag
  class:list={[
    'font-extrabold text-[32px] leading-tight lg:text-5xl mb-6',
    className
  ]}
  {...props}
>
  <slot></slot>
</Tag>